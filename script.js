console.log()
window.console.log(window) // window is a global object that represents the browser window or tab that is currently running the JavaScript code
// whole HTML page becomes an object called document
console.log(document.body)
document.body.style.background = "yellow"